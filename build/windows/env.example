# ============================================
# 服务器配置
# ============================================
# Gin运行模式: debug, release, test
GIN_MODE=release

# 服务器端口
PORT=80

# ============================================
# 数据库配置
# ============================================
# 数据库类型: mysql / sqlite / postgres（不填默认为 sqlite）
DB_TYPE=sqlite

# ----- 当 DB_TYPE=sqlite 时 -----
# SQLite 数据库路径（相对路径或绝对路径）
# 不设置时默认: 程序目录/data/nbutest.db3
SQLITE_PATH=
# SQLite 数据库密码（未加密留空）
SQLITE_PWD=

# ----- 当 DB_TYPE=mysql 时 -----
# MySQL 连接串，示例: user:password@tcp(localhost:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local
# MYSQL_DSN=user:password@tcp(localhost:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local
# 也可使用 MYSQL_URL（与 MYSQL_DSN 二选一）
# MYSQL_URL=user:password@tcp(localhost:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local

# ----- 当 DB_TYPE=postgres 时 -----
# PostgreSQL 连接串
# POSTGRES_DSN=host=127.0.0.1 user=postgres password=xxx dbname=postgres port=5432 sslmode=disable



# ============================================
# 调试和日志配置
# ============================================
# 是否开启调试模式（ON/OFF）
# 开启后会在 :3016 端口启动 pprof 调试服务
MAIN_DEBUG=OFF

# 日志级别: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ============================================
# 其他配置（已注释，需要时取消注释）
# ============================================
# Redis连接地址
# REDIS_URL=127.0.0.1:6379

# MQTT连接地址
# MQTT_URL=tcp://localhost:1883
